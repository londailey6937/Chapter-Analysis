/**
 * Backup of root `index.ts` created as `index.types_backup.ts` to remove duplicate
 * type definitions while keeping a safe copy. This file was automatically copied
 * from the original root `index.ts` and can be deleted later if everything passes.
 *
 * NOTE: Content is the original file snapshot. Keep as backup.
 */

export * from "./index";
